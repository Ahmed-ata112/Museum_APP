import 'package:flutter/material.dart';
class SouvenirSale extends StatelessWidget {
  static String id='SouvenirSale';
  @override
  Widget build (BuildContext context) {
    return Scaffold(
      //body: StreamBuilder
        //(

      //  ),
    );
  }
}